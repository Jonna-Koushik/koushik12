results = {}
num_students = int(input())
for i in range(num_students):
    name, result = input().split()
    results[name] = result
names_to_lookup = input().split()
for name in names_to_lookup:
    if name in results:
        print(name, results[name], 'the exam')
    else:
        print('No Result Found')
